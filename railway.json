{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "code-server --bind-addr 0.0.0.0:$PORT --auth none --disable-telemetry",
    "restartPolicyType": "ON_FAILURE"
  }
}
